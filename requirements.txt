# Free Discord Account Generator - Requirements

# Core dependencies
discord.py>=2.0.0
aiohttp>=3.8.0
asyncio

# Web automation (for free services)
selenium>=4.0.0
webdriver-manager>=3.8.0

# HTTP requests
requests>=2.28.0
aiohttp>=3.8.0

# Data handling
fake-useragent>=1.1.0
python-dotenv>=0.19.0

# Optional dependencies for enhanced features
# These are not required for basic free functionality
# twocaptcha>=0.0.1  # Optional: for paid CAPTCHA solving
# sms-activate-api>=1.0.0  # Optional: for paid SMS verification

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
